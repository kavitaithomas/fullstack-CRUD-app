# Dependencies

### Express

for http server
npm install express

### Dotenv

for environment variable management
npm install dotenv

### nodemon

for automatic server restarts in dev
npm install --save-dev nodemon

To run globally:
nodemon index.js

To run locally:
npx nodemon
